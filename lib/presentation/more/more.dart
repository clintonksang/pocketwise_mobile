

 
 
import 'package:flutter/material.dart';

import '../../utils/constants/textutil.dart';

class More extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('More Page', style: AppTextStyles.normal),
    );
  }
}